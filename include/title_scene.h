#ifndef TITLE_SCENE_H
#define TITLE_SCENE_H

// Title screen. A continues the current campaign day (session state).
// Start opens credits (H-03). SELECT resets to Day 1 (explicit new game).
// Does not reset on entry.
void run_title_scene();

#endif

