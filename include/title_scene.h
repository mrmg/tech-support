#ifndef TITLE_SCENE_H
#define TITLE_SCENE_H

// Placeholder title screen. Returns when the player confirms with A
// (Start is accepted as a secondary confirm so Start can stay reserved elsewhere).
void run_title_scene();

#endif
